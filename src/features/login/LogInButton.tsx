const LogInButton = () => {
  return (
    <button
      className='mt-6 py-2.5 px-12 bg-gray-500 text-sm lg:text-base text-white hover:bg-gray-600 disabled:bg-gray-400 rounded-lg transition-all duration-300'
      type='submit'
    >
      Confirm
    </button>
  );
};

export default LogInButton;
